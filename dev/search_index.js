var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API","page":"API","text":"","title":"API"},{"category":"section","location":"api/#Core-Entry-Points","page":"API","text":"","title":"Core Entry Points"},{"category":"function","location":"api/#CausalEstimate.estimate","page":"API","text":"estimate(psi, method, data; kwargs...)\n\nEstimate a causal estimand under the default identification assumptions implied by psi and method.\n\n\n\n\n\nestimate(psi, id, method, data; kwargs...)\n\nEstimate a causal estimand under an explicit identification strategy.\n\n\n\n\n\n","title":"CausalEstimate.estimate"},{"category":"function","location":"api/#CausalEstimate.identify","page":"API","text":"identify(id, psi; kwargs...)\n\nResolve the identification strategy for psi. Graph-based identification is provided through an optional CausalGraphs.jl extension.\n\n\n\n\n\n","title":"CausalEstimate.identify"},{"category":"section","location":"vignettes/03_graph_based_estimation/#Graph-Based-Estimation","page":"Graph-Based Estimation","text":"CausalEstimate.jl integrates with CausalGraphs.jl to support graph-identified causal effects. Rather than specifying confounders manually, you build an ADMG and let the package discover the valid adjustment set from the graph structure.","title":"Graph-Based Estimation"},{"category":"section","location":"vignettes/03_graph_based_estimation/#Installation","page":"Graph-Based Estimation","text":"Install both packages:\n\nusing Pkg\nPkg.add(url=\"https://github.com/xiangao/CausalEstimate.jl\")\nPkg.add(url=\"https://github.com/xiangao/CausalGraphs.jl\")","title":"Installation"},{"category":"section","location":"vignettes/03_graph_based_estimation/#A-Simple-Backdoor-Example","page":"Graph-Based Estimation","text":"Suppose you have a graph X → A → Y ← X. The confounder X creates a backdoor path from A to Y. Conditioning on X blocks this path and identifies the ATE.\n\nusing CausalEstimate\nusing CausalGraphs\nusing DataFrames\nusing Random\n\nRandom.seed!(7)\n\nn = 1000\nX = randn(n)\nA = Int.(rand(n) .< 1.0 ./ (1.0 .+ exp.(-X)))   # P(A=1|X) = expit(X)\nY = 1.5 .* A .+ 0.8 .* X .+ randn(n)             # true ATE = 1.5\n\ndf = DataFrame(Y = Y, A = A, X = X)\n\nr(x) = round(x, sigdigits = 4)\n\ng = make_graph(\n    vertices = [:A, :Y, :X],\n    di_edges = [(:X, :A), (:X, :Y), (:A, :Y)],\n);\n\nPass the graph as a GraphID identification layer between the estimand and the estimator:\n\npsi = ATE(outcome = :Y, treatment = :A)\nresult = estimate(psi, GraphID(graph = g), AIPW(crossfit = 2), df)\n\nr(estimate(result))\n\nr.(confint(result))\n\nThe adjustment set that GraphID discovered:\n\nresult.components[:adjustment_set]\n\nGraphID reads the Markov pillow of the treatment from the graph — the minimal valid adjustment set implied by the backdoor criterion — and passes it to the estimator. You never need to list confounders by hand.","title":"A Simple Backdoor Example"},{"category":"section","location":"vignettes/03_graph_based_estimation/#How-Graph-Identification-Works","page":"Graph-Based Estimation","text":"CausalGraphs.jl implements the general fixability hierarchy for ADMGs:\n\nGraph property Identification strategy\na-fixable Backdoor / Markov-pillow adjustment\np-fixable Front-door / NPS TMLE\nnested-fixable ANIPW / nested IPW\nID-algorithm identified Symbolic Pearl-Shpitser functional\n\nGraphID currently routes a-fixable (backdoor) effects. More graph routes can be added behind the same estimate interface.","title":"How Graph Identification Works"},{"category":"section","location":"vignettes/03_graph_based_estimation/#Switching-Estimators-Behind-GraphID","page":"Graph-Based Estimation","text":"You can use TMLE instead of AIPW without changing anything else:\n\nresult_tmle = estimate(psi, GraphID(graph = g), TMLE(crossfit = 2), df)\n\n(\n    estimate = r(estimate(result_tmle)),\n    ci       = r.(confint(result_tmle)),\n)","title":"Switching Estimators Behind GraphID"},{"category":"section","location":"vignettes/03_graph_based_estimation/#Comparing-Manual-and-Graph-Identified-Adjustment","page":"Graph-Based Estimation","text":"When confounders are known, both approaches should agree:\n\npsi_manual = ATE(outcome = :Y, treatment = :A, confounders = [:X])\nresult_manual = estimate(psi_manual, AIPW(crossfit = 2), df)\n\n(\n    graph_identified = r(estimate(result)),\n    manual           = r(estimate(result_manual)),\n)","title":"Comparing Manual and Graph-Identified Adjustment"},{"category":"section","location":"vignettes/03_graph_based_estimation/#ATT-with-GraphID","page":"Graph-Based Estimation","text":"The same graph-based identification works for ATT:\n\npsi_att = ATT(outcome = :Y, treatment = :A)\nresult_att = estimate(psi_att, GraphID(graph = g), AIPW(crossfit = 2), df)\n\nr(estimate(result_att))","title":"ATT with GraphID"},{"category":"section","location":"vignettes/03_graph_based_estimation/#Scope","page":"Graph-Based Estimation","text":"Implemented now:\n\nGraph-implied backdoor adjustment for ATE\nGraph-implied backdoor adjustment for ATT\n\nPlanned next:\n\np-fixable / front-door routing through GraphID\nBroader ID-algorithm-backed estimation for nested-fixable effects\nFull ADMG result summaries (ACE, confidence intervals, hedge diagnostics)\n\nFor full ADMG estimation (p-fixable, nested-fixable, ID-algorithm effects), see CausalGraphs.jl, which provides the complete estimation pipeline directly.","title":"Scope"},{"category":"section","location":"vignettes/01_getting_started/#Getting-Started-with-CausalEstimate.jl","page":"Getting Started","text":"CausalEstimate.jl provides a unified interface for estimating causal effects. The workflow has three steps:\n\nDefine an estimand — what causal quantity do you want? (ATE, ATT, …)\nChoose an estimator — how should it be estimated? (TMLE, AIPW, …)\nCall estimate — the package handles nuisance fitting, cross-fitting, and inference.","title":"Getting Started with CausalEstimate.jl"},{"category":"section","location":"vignettes/01_getting_started/#Installation","page":"Getting Started","text":"using Pkg\nPkg.add(url=\"https://github.com/xiangao/CausalEstimate.jl\")","title":"Installation"},{"category":"section","location":"vignettes/01_getting_started/#A-First-ATE-Example","page":"Getting Started","text":"We simulate a simple observational study: a scalar confounder W, a binary treatment A, and a continuous outcome Y. The true ATE is 2.\n\nusing CausalEstimate\nusing DataFrames\nusing Random\n\nRandom.seed!(42)\n\nn = 1000\nW = randn(n)\nA = Int.(rand(n) .< 1.0 ./ (1.0 .+ exp.(-W)))  # P(A=1|W) = expit(W)\nY = 2.0 .* A .+ W .+ randn(n)                   # true ATE = 2\n\ndf = DataFrame(Y = Y, A = A, W = W)\nfirst(df, 5)\n\nDefine the estimand and estimate with TMLE using 2-fold cross-fitting:\n\nr(x) = round(x, sigdigits = 4)\n\npsi = ATE(outcome = :Y, treatment = :A, confounders = [:W])\nresult = estimate(psi, TMLE(crossfit = 2), df)\n\nr(estimate(result))\n\nExtract inferential summaries:\n\nr.(confint(result))\n\nr(pvalue(result))\n\nBoth TMLE and AIPW are doubly robust: they remain consistent if either the outcome model or the propensity score model is correctly specified (but not necessarily both). TMLE additionally applies a targeting step that makes the bias-variance tradeoff more favorable in finite samples.","title":"A First ATE Example"},{"category":"section","location":"vignettes/01_getting_started/#Switching-Estimators","page":"Getting Started","text":"The estimate dispatch is the same regardless of which estimator you choose:\n\nresult_aipw = estimate(psi, AIPW(crossfit = 2), df)\nr(estimate(result_aipw))\n\nBoth should be close to the true ATE of 2. The estimand object psi never changes — only the estimator does.","title":"Switching Estimators"},{"category":"section","location":"vignettes/01_getting_started/#ATT:-Effect-on-the-Treated","page":"Getting Started","text":"For the Average Treatment Effect on the Treated, use ATT. The identification assumption is the same (no unmeasured confounding given W), but the estimand weights by the treated subpopulation.\n\nY0 = W .+ randn(n)\nY1 = 1.5 .+ W .+ randn(n)\nY_att = A .* Y1 .+ (1 .- A) .* Y0   # potential outcomes model, true ATT = 1.5\ndf_att = DataFrame(Y = Y_att, A = A, W = W)\n\npsi_att = ATT(outcome = :Y, treatment = :A, confounders = [:W])\nresult_att = estimate(psi_att, AIPW(crossfit = 2), df_att)\n\nr(estimate(result_att))","title":"ATT: Effect on the Treated"},{"category":"section","location":"vignettes/01_getting_started/#Result-Components","page":"Getting Started","text":"Every CausalEstimateResult carries a components dictionary with intermediate estimates. For TMLE you can compare the one-step correction:\n\nr(estimate(result.components[:onestep]))\n\nFor AIPW you can inspect the treated and control potential outcome means:\n\n(\n    treated = r(estimate(result_aipw.components[:treated])),\n    control = r(estimate(result_aipw.components[:control])),\n)","title":"Result Components"},{"category":"section","location":"vignettes/01_getting_started/#What-Is-Implemented","page":"Getting Started","text":"Estimand Estimator Status\nATE TMLE ✓\nATE AIPW ✓\nATT AIPW ✓\nATE / ATT GraphID + AIPW / TMLE ✓\n\nSee the Effect Estimators and Graph-Based Estimation vignettes for details.","title":"What Is Implemented"},{"category":"section","location":"#CausalEstimate.jl","page":"Home","text":"CausalEstimate.jl is a unified API for causal effect estimation in Julia, combining the nonparametric estimation methods of NPCausal.jl with the targeted learning machinery of TMLE.jl under one consistent interface.\n\nThe package implements doubly robust semiparametric estimators — TMLE and AIPW — with cross-fitting, and an optional graph-based identification layer via CausalGraphs.jl.","title":"CausalEstimate.jl"},{"category":"section","location":"#Implemented-Routes","page":"Home","text":"Estimand Identification Estimator\nATE Unconfounded TMLE, AIPW\nATT Unconfounded AIPW\nATE, ATT GraphID (backdoor) TMLE, AIPW","title":"Implemented Routes"},{"category":"section","location":"#API","page":"Home","text":"estimate(estimand, estimator, data)\nestimate(estimand, identification, estimator, data)\n\nThe returned CausalEstimateResult exposes:\n\nestimate(result)    # Float64 point estimate\nconfint(result)     # (lower, upper) 95% CI\npvalue(result)      # two-sided p-value\nresult.components   # Dict of intermediate estimates","title":"API"},{"category":"section","location":"#Vignettes","page":"Home","text":"Getting Started — ATE and ATT with TMLE and AIPW\nEffect Estimators — when to use TMLE vs AIPW, result components\nGraph-Based Estimation — graph-implied identification with CausalGraphs.jl","title":"Vignettes"},{"category":"section","location":"vignettes/02_effect_estimators/#Effect-Estimators","page":"Effect Estimators","text":"This vignette compares the estimators available in CausalEstimate.jl and explains when to use each one.","title":"Effect Estimators"},{"category":"section","location":"vignettes/02_effect_estimators/#Setup","page":"Effect Estimators","text":"We work with a two-confounder data-generating process. The true ATE is 2.0 and the true ATT is 1.25.\n\nusing CausalEstimate, DataFrames, Random\n\nRandom.seed!(123)\nn = 1200\nw1 = rand(Bool, n)\nw2 = randn(n)\npA = 1.0 ./ (1.0 .+ exp.(-(-0.3 .+ 0.6 .* Float64.(w1) .+ 0.4 .* w2)))\nA = Int.(rand(n) .< pA)\n\nY_cont = 2.0 .* A .+ 0.5 .* Float64.(w1) .+ w2 .+ randn(n)\ndf_cont = DataFrame(Y = Y_cont, A = A, w1 = Float64.(w1), w2 = w2)\n\npsi_ate = ATE(outcome = :Y, treatment = :A, confounders = [:w1, :w2])\n\nr(x) = round(x, sigdigits = 4)\n\nfirst(df_cont, 5)","title":"Setup"},{"category":"section","location":"vignettes/02_effect_estimators/#TMLE-for-ATE","page":"Effect Estimators","text":"Targeted Maximum Likelihood Estimation fits nuisance parameters (outcome regression and propensity score) and then applies a one-dimensional targeting step that solves the efficient score equation. This targeting step improves finite-sample behavior relative to a naive plug-in estimator.\n\ntmle_result = estimate(psi_ate, TMLE(crossfit = 2), df_cont)\n\n(\n    estimate  = r(estimate(tmle_result)),\n    ci        = r.(confint(tmle_result)),\n    one_step  = r(estimate(tmle_result.components[:onestep])),\n)\n\nresult.components[:onestep] gives the pre-targeting AIPW one-step estimate, useful for diagnosing how much the targeting step moved the estimate.","title":"TMLE for ATE"},{"category":"section","location":"vignettes/02_effect_estimators/#AIPW-for-ATE","page":"Effect Estimators","text":"Augmented Inverse Probability Weighting (also known as the doubly robust one-step estimator) corrects the plug-in estimator with an influence-function term. It is consistent if either the outcome model or the propensity model is correctly specified.\n\naipw_result = estimate(psi_ate, AIPW(crossfit = 2), df_cont)\n\n(\n    estimate      = r(estimate(aipw_result)),\n    ci            = r.(confint(aipw_result)),\n    treated_mean  = r(estimate(aipw_result.components[:treated])),\n    control_mean  = r(estimate(aipw_result.components[:control])),\n)\n\nThe treated and control components give the separately estimated potential outcome means E[Y(1)] and E[Y(0)]. The ATE is their difference.","title":"AIPW for ATE"},{"category":"section","location":"vignettes/02_effect_estimators/#AIPW-for-ATT","page":"Effect Estimators","text":"The ATT weights by the treated subpopulation rather than the full population. Use the same AIPW estimator with the ATT estimand:\n\nY0 = 0.5 .* Float64.(w1) .+ w2 .+ randn(n)\nY1 = 1.25 .+ 0.5 .* Float64.(w1) .+ w2 .+ randn(n)\nY_att = A .* Y1 .+ (1 .- A) .* Y0\ndf_att = DataFrame(Y = Y_att, A = A, w1 = Float64.(w1), w2 = w2)\n\npsi_att = ATT(outcome = :Y, treatment = :A, confounders = [:w1, :w2])\natt_result = estimate(psi_att, AIPW(crossfit = 2), df_att)\n\n(\n    estimate            = r(estimate(att_result)),\n    ci                  = r.(confint(att_result)),\n    treated_mean        = r(estimate(att_result.components[:treated_mean])),\n    counterfactual_ctrl = r(estimate(att_result.components[:counterfactual_control_mean])),\n)\n\ncounterfactual_control_mean is the estimated mean of Y(0) among the treated — that is, what their outcome would have been under control.","title":"AIPW for ATT"},{"category":"section","location":"vignettes/02_effect_estimators/#Choosing-Between-TMLE-and-AIPW","page":"Effect Estimators","text":"Property TMLE AIPW\nDoubly robust ✓ ✓\nTargeted update (targeting step) ✓ —\nRespects outcome bounds ✓ (via logistic targeting) —\nSimpler implementation — ✓\nAccess to intermediate estimates via :onestep via :treated, :control\n\nIn most settings the two estimators give similar results. TMLE is preferred when the outcome is bounded (binary or in [0,1]) or when you want the additional finite-sample guarantee from the targeting step. AIPW is a natural first choice for continuous unbounded outcomes.","title":"Choosing Between TMLE and AIPW"},{"category":"section","location":"vignettes/02_effect_estimators/#Result-Object","page":"Effect Estimators","text":"All calls return a CausalEstimateResult:\n\ntypeof(att_result)\n\nThe fields are:\n\npropertynames(att_result)\n\nAccess the primary estimate directly via:\n\natt_result.primary","title":"Result Object"}]
}
